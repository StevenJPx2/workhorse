# Sandbox image build context = repo root (wrangler image_build_context),
# but the image only COPYs worker/sandbox/ + plugins/. Keep the context lean.
*
!worker
worker/*
!worker/sandbox
!worker/sandbox/**
!plugins
!plugins/**
plugins/*/node_modules
