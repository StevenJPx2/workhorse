# lib

Internal utilities and infrastructure, not directly part of the public API.

## Modules

- **hooks/** — Event pub/sub system via mitt

Each subdirectory is aliased (e.g., `#lib/hooks`) for clean imports.
