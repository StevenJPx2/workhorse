import { definePlugin } from "../../../../define.ts";

export default definePlugin({
  manifest: {
    name: "directory-plugin",
    version: "1.0.0",
    description: "Plugin in a directory",
  },
  setup() {},
});
