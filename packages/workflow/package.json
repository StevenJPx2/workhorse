{
  "name": "@workhorse/workflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hard-coded, eval-tested workflow definitions plus the stage-assembly helpers the worker's WorkflowContext uses. No interpreter, no spec registry.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@workhorse/test-utils": "workspace:*",
    "typescript": "^7.0.2"
  }
}
