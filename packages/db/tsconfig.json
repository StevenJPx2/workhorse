{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // vitest-pool-workers types declare the `cloudflare:test` module the test
    // setup imports; without it tsc can't see applyD1Migrations or D1Migration.
    "types": ["@cloudflare/workers-types", "@cloudflare/vitest-pool-workers/types"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
