{
  "name": "@workhorse/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Workhorse plugin contract — types only. Plugins depend on this and nothing else in the workspace; the worker is the sole composition point.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@flue/runtime": "^1.0.0-beta.9"
  },
  "devDependencies": {
    "@cloudflare/sandbox": "^0.12.3",
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^7.0.2"
  }
}
