{
  "name": "@workhorse/semindex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Semantic index toolkit: defineIndex({name,id,toText}) → upsert/remove/query over Workers AI embeddings + one namespaced Vectorize index.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^7.0.2"
  }
}
