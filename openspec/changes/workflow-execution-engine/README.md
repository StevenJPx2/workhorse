# workflow-execution-engine

Replace the Workflow.run stub with a real looping stage machine (exits + when-rules + status transitions + epilogue handoff), add a BaseAgent event-stream engine plus a ScriptedAgent test double, and wire the Harness to drive a bones-shaped Agent. Follow-up to orchestrator-boilerplate.
