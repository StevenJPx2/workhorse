import type {
{{pascalCase name}}Service } from "../service"; export function
{{camelCase name}}Tools(service:
{{pascalCase name}}Service) { return [ // TODO: add tools (use `aube run
generate tool` to scaffold one) ]; }