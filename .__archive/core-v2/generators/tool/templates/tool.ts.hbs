import z from "zod"; import { defineTool, type
{{pascalCase service}}T } from "#schema"; export function
{{camelCase name}}Tool(items: () => readonly
{{pascalCase service}}T[]) { return defineTool({ annotations: { // TODO: pick
hints that match the tool's behaviour destructive_hint: false, idempotent_hint:
true, open_world_hint: false, read_only_hint: true, title: "{{titleCase name}}",
}, description: "TODO: describe what
{{snakeCase name}}
does and when the agent should call it.", execute: async ({ name }, ctx) => {
const item = items().find((candidate) => candidate.name === name); if (!item) {
return { error: `No
{{camelCase service}}
named "${name}".`, ok: false, }; } // TODO: implement the tool behaviour return
{ ok: true, output: String(item), }; }, input: z.object({ name:
z.string().describe("The
{{camelCase service}}
name"), }), name: "{{snakeCase name}}", }); }