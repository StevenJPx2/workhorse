{
  "$comment": "Pi extension PACKAGES baked into the sandbox image (pin versions for fast-moving deps) + Pi settings. Plugin sandbox tools are NOT listed here — install.mjs discovers every plugins/<name>/extension.ts in the workspace automatically. Tools are OFF inside workflow runs unless a stage's tools[] allowlist names them (custom tools need an object spec with a classification) — stage gating is enforced by pi-workflow.",
  "packages": [
    "npm:@gotgenes/pi-anthropic-auth",
    "npm:@agwab/pi-workflow@0.8.0",
    "npm:@cortexkit/aft-pi",
    "npm:@cortexkit/pi-magic-context@0.32.0"
  ],
  "settings": {
    "defaultProvider": "anthropic",
    "defaultModel": "claude-sonnet-4-6"
  }
}
