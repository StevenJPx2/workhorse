{
  "$comment": "Pi extension PACKAGES baked into the image (auth, magic context, aft-pi) + Pi settings. Under the flue-first engine there are NO sandbox tool extensions — plugin tools run worker-side (plugins/<name>/tools/) over the sandbox handle, gated per stage by the workflow def's tools[] allowlist.",
  "packages": [
    "npm:@gotgenes/pi-anthropic-auth",
    "npm:@cortexkit/aft-pi",
    "npm:@cortexkit/pi-magic-context@0.32.0"
  ],
  "settings": {
    "defaultProvider": "anthropic",
    "defaultModel": "claude-sonnet-4-6"
  }
}
