/**
 * Event formatter for OpenCode SDK events
 *
 * Pure functions for transforming SDK events into displayable messages.
 */

export type { StreamMessage } from "./types.ts";
export { formatEvent } from "./format-event.ts";
